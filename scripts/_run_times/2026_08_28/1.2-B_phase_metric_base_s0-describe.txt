实验 / Experiment: 1.2-B_phase_metric_base_s0
model_name: ProMoE_TC_B  |  config: configs/004_ProMoE_B_phase_metric_base_s0.yaml
基线 / Baseline: 基础 ProMoE-TC（conditional cond/uncond routing、静态 `cluster_centers` cosine routing、shared expert 和 routing contrastive guidance）；直接父变体 / Parent: 基础 ProMoE-TC

核心改动 / Core changes（相对基线，重点优先）:
1. 严格 fresh control：`phase_metric_config.enabled: False`，因此模型不创建 `PhaseConditionedRoutingMetric`，路由和 forward 路径保持基础 ProMoE-TC 的静态 prototype affinity。
2. 配对初始化与预算 / paired initialization and budget：global seed 为 0，global batch 为 256，lr 为 `1e-4`，`num_steps: 501000`，使用与 phase metric 实验相同的本地 encoded latents、worker 数和采样评估协议；该 control 从独立空输出目录 step 0 开始，不加载已有 checkpoint。
3. 对比用途 / comparison role：本配置作为 native phase metric 的同 seed Base 对照，报告 300K/500K、CFG 1.0/1.5 的 OpenAI evaluator 结果，并与 routed expert usage、路由 margin/entropy 等诊断一并比较。
