实验 / Experiment: 3.1-B_tc_proto_t_residual_phase_control_s0_v2
model_name: ProMoE_TC_B_proto_t  |  config: configs/004_ProMoE_B_proto_t_residual_phase_control_s0_v2.yaml
基线 / Baseline: 基础 ProMoE-TC（cond/uncond 分流、静态 `cluster_centers` cosine routing、shared expert 和 routing contrastive guidance）；直接父变体 / Parent: Proto-T residual（同一机制的旧定义，本 v2 使用独立输出桶和新槽位执行 fresh run）

核心改动 / Core changes（相对基线，重点优先）:
1. 时间步条件原型 / timestep-conditioned prototypes：每个 MoE block 的 `PrototypeMLP` 根据该样本的 `t_emb` 和静态 `cluster_centers` 生成逐样本 prototype，再用它与 token 做 cosine routing。因此改动直接作用于 routed-expert 选择，不使用 REPA/DINO teacher，也不增加 `top_k` 或激活专家数。
2. residual 机制对照 / residual control：`proto_t_update_mode: residual` 使用 `prototype_t = cluster_centers + delta`；`delta` 的 `fc2` 为零初始化，因此初始化时 prototype 精确等于静态中心。相对 `direct` sibling，本配置没有额外的可训练 `proto_proj` skip；`proto_t_init_seed: 1729` 将新增 MLP 的初始化与 Base 公共参数的 RNG 隔离。
3. fresh 完整协议 / fresh full schedule：不配置 `initial_checkpoint_path`，独立输出到 `/home/dev/promoe-runs/ProMoE_TC_B_proto_t/004_ProMoE_B_proto_t_residual_phase_control_s0_v2/`，从 step 0 训练；seed 0、global batch 256、lr `1e-4`、16 workers，训练到 500K，并在 300K/500K 对 CFG 1.0/1.5 各生成 50K 图像后调用 OpenAI evaluator。
