实验 / Experiment: 7.2-B_dagfuse_region_resid_softmax
model_name: ProMoE_TC_B_dagfuse_region  |  config: configs/004_ProMoE_B_dagfuse_region_resid_softmax.yaml
基线 / Baseline: 基础 ProMoE-TC（静态 cluster_centers 原型 + 两步路由 + shared 专家 + contrastive）；直接父变体 / Parent: 姊妹实验 7.1（region_resid_dag）——dag↔softmax 机制消融对照

核心改动 / Core changes（相对基线，重点优先）:
1. 区域残差流 Block-AttnRes / region residual-stream Attention-Residual（headline）：`region_attach: resid`——在每个区域边界（新区域第一个 block 之前，blocks 3/6/9），用之前各区域代表经 DiT 级 zero-init `SharedAugmentModule` 给**主干残差流 x** 加注意力残差，作用于全部 token。
2. `fuse_mech: softmax` 聚合机制：缩放点积注意力（fp32 softmax，含 self-edge），对照姊妹 7.1 的 `dag`（门控 value-sum）——同一 source 集合下的机制消融轴。
3. 相对 region_shared 臂（attach-point 消融）：补强对象是主干 x 而非 shared 输出。**诚实标注**：zero-init → step-0 与 base 逐比特一致，但训练后改写 x → **行为上影响下游路由输入**（全家唯一非路由不变臂）。`region_size: 3`。
