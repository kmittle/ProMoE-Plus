"""Git provenance helpers kept from the credit-rate research line.

The learning-credit redistribution study itself was removed: its only
experiments continued from another run's checkpoint, which this project does
not accept as a clean ablation (see README.md and CLAUDE.md).  Only
`git_provenance.py` survives, because `train.py`'s strict-provenance manifest
and `analyses/expert_update_budget/audit.py` both pin it by path and sha256.
"""
