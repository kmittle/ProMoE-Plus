interface:
  display_name: "/init"
  short_description: "Build or refresh AGENTS.md from repo context"
  default_prompt: "Use $init to scan this repository and build or update AGENTS.md from the current project structure, commands, and conventions."
policy:
  allow_implicit_invocation: false
